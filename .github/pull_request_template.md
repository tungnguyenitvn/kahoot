## Related issue / ADR
<!-- Issue number, and the ADR if a decision changed. -->

## Goal / non-goals / acceptance IDs
<!-- What must be true after the merge, by acceptance ID (ROOM-03, LIVE-05 ...). What is explicitly out of scope. -->

## Required context and affected boundaries
<!-- Links to the feature, module, contract and ADR pages you used; the boundaries (REST, WS, Redis, SQL) you touched. -->

Feature / module / contracts / ADR links:

## What changed
<!-- Why before what: the choice you made and the alternatives you rejected. The diff already shows the what. -->

## Behavior and docs
- [ ] Current feature/domain document updated if behavior changed
- [ ] No behavior change

## Verification
- [ ] `./scripts/verify` passed
- [ ] Other relevant checks (describe below)

<!-- Copy the exact commands and results from the terminal, then the environment (OS, Docker or Node version) and the revision you ran them on. -->

Record exact command, result, environment and revision/artifact. Mark checks NOT RUN
when unavailable; do not tick full verification based on smoke tests.

## Change status
<!-- Draft while iterating, Accepted once scope and IDs are agreed, Implemented when the code is complete, Verified only after the gates ran on the final revision. -->

Draft / Accepted / Implemented / Verified (choose and justify)

## Notes / unverified areas
<!-- Remaining risks, compatibility impact, rollback or migration needs, reviewer approval. -->
